# Local BorealOS configuration, copied and edited from the tracked example at
# config/borealos.env.example. Keep machine-specific values out of git.
/config/borealos.env

# A locally initialized LineageOS/AOSP workspace should never be committed; the
# full Android source tree is fetched on demand, not vendored here.
/borealos-lineageos/
